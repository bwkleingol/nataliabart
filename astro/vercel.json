{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "astro",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/locales/(.*)", "dest": "/locales/$1" }
  ]
}
